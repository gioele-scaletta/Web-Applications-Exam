  <> <Row>
            <h1>
            {props.surveytitle}
            {console.log(props.questions)}
            {console.log(props.responses)}

            </h1>
        </Row>
      
      {props.questions.sort( (a,b) => (a.qnum<b.qnum) ? a : b).map((q) => q.open ? 
           
            <OpenAnswerForm key={q.qnum} id={q.qnum} question={q.qtext} add={undefined} response={undefined/*props.responses.map( (r) => (r.rnum===users[currentUser] && r.qnum===q.qnum) ? r: null).sort((a,b) => (a.qnum<b.qnum) ? a : b).rtext*/}
            />
            :
            <CloseAnswerForm key={q.qnum} id={q.qnum} question={q.qtext} optional={q.optional} single={q.single} add={undefined} response={undefined/*props.responses.map((r)=>(r.rnum===users[currentUser] && r.qnum===q.qnum) ? r :null).sort((a,b) => (a.qnum<b.qnum) ? a : b).rtext*/}
            />
        
        )}
        <Row>
           <Button onClick={()=>next()}>Next </Button>
        </Row>
        <Row>
            <Link to="/admin"><Button>Back </Button> </Link> 
        </Row>
        </> 